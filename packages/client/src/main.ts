// ===========================================
// ESOP Wars v2 - Client Entry Point
// ===========================================

import { init } from './app';

// Initialize app when DOM is ready
document.addEventListener('DOMContentLoaded', () => {
  init();
});
