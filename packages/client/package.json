{
  "name": "@esop-wars/client",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "deploy": "npm run build && wrangler pages deploy dist --project-name=esop-wars --commit-dirty=true"
  },
  "dependencies": {
    "@esop-wars/shared": "*",
    "solid-js": "^1.9.11"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vite": "^6.0.0",
    "vite-plugin-solid": "^2.11.10"
  }
}
